package utils;

interface IOperasiTigaDimensi {
    int volumeKubus(double sisi);
    int volumeBalok(int panjang, int lebar, int tinggi);
    double volumeTabung(double phi, double jariJari, int tinggi);
}
